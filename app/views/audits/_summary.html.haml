.hidden-md.hidden-sm.hidden-xs
  = format_field("Name", audit.name)
  = format_field("Auditor", audit.auditor)
  %hr
  = format_field("Active", format_as_boolean(audit.active))
  = format_field("Schedule", format_as_text(audit.schedule))
  %hr
  = format_field("Last Run", format_as_date(audit.last_run))
  = format_field("Created On", format_as_date(audit.created_at))
  = format_field("Last Updated", format_as_date(audit.updated_at))
.hidden-lg
  .col-sm-4
    = format_field("Name", audit.name)
  .col-sm-4
    = format_field("Active", format_as_boolean(audit.active))
    = format_field("Schedule", format_as_text(audit.schedule))
  .col-sm-4
    = format_field("Last Run", format_as_date(audit.last_run))
    = format_field("Created On", format_as_date(audit.created_at))
    = format_field("Last Updated", format_as_date(audit.updated_at))
