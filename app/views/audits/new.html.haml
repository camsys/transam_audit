.col-sm-12.col-md-10.col-md-offset-1.col-lg-8.col-lg-offset-2
  .page-header
    %h2 New Audit

  = render(:partial => 'shared/form_errors', :locals => {:obj => @audit}) unless @audit.errors.empty?

  = render 'form'

= render 'scripts'
